interface:
  display_name: "GPU Shader Toolkit"
  short_description: "Inspect and port captured GPU shader binaries"
  default_prompt: "Use $gpu-shader-toolkit to inspect a captured shader, preserve its evidence, and plan a verifiable cross-API port."
